export default function SettingsPage() {
  return (
    <div className="hna-grid">
      <div className="hna-card">
        <h3>Settings</h3>
        <p>Environment, API base URL, theme, and system toggles.</p>
      </div>
    </div>
  );
}
