import GrnListPage from "./GrnListPage";

// Draft management now uses the same ERP-grade list shell.
// The route remains for compatibility with existing navigation.
export default function GrnDraftListPage() {
  return <GrnListPage />;
}
