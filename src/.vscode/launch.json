{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Chrome (frontend)",
      "url": "http://localhost:5174",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
