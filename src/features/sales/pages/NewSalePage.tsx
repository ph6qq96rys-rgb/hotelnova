import PosPage from "./PosPage";

/**
 * Backward-compatible route.
 * RestaurantFNB now uses one Toast/Square-style POS terminal for sale entry.
 */
export default function NewSalePage() {
  return <PosPage />;
}
